<%= form_for(@testpage) do |f| %>
  <% if @testpage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@testpage.errors.count, "error") %> prohibited this testpage from being saved:</h2>

      <ul>
      <% @testpage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
